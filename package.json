{
    "name": "stirling-pdf-vercel",
    "version": "1.0.0",
    "description": "Stirling PDF deployment for Vercel",
    "main": "index.js",
    "scripts": {
      "start": "node index.js",
      "build": "mkdir -p public && cp -r ./StirlingPDF/customFiles/* ./public/ || true"
    },
    "dependencies": {
      "express": "^4.18.2",
      "http-proxy-middleware": "^2.0.6"
    },
    "engines": {
      "node": ">=14.0.0"
    }
  }